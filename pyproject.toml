[project]
name = "watchtower"
version = "0.1.0"
requires-python = ">=3.11,<3.14"
dependencies = [
    "httpx>=0.27.0",
    "ipykernel>=7.3.0",
    "pygame>=2.6.0",
]

[project.scripts]
watchtower = "watchtower.app:main"

[tool.setuptools]
packages = ["watchtower"]
